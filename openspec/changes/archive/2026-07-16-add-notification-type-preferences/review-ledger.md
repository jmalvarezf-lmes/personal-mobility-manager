# Review Ledger — add-notification-type-preferences

4R parallel review (risk, resilience, readability, reliability) of the full implementation diff, run before commit/PR per repo review policy (diff >400 lines, touches login flow).

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| R1 | risk | n/a | — | info | No findings. Authorization, config validation, SQL parameterization, and secrets handling all clean across the new endpoints, repo, migrations, and login change. |
| R4-001 | resilience | `notification_dispatch_handler.py` (`handle`) | BLOCKER | fixed | No try/except around `handle()`, unlike sibling `SerTicketTriggerHandler` which documents needing exactly this isolation (unguarded synchronous event publisher; DB error would 500 the push-location-ingestion endpoint even though the location write already succeeded). New preference-repo DB call increases exposure on this unguarded path. |
| R4-002 | resilience | `authenticate_google_user.py` (`ensure_defaults` call) | CRITICAL | fixed | New `ensure_defaults()` call is synchronous and unguarded in the Google login flow. A DB error there fails the entire login even though the user record already upserted successfully — couples an unrelated preferences subsystem to core auth availability. |
| R4-003 | resilience | `presentation/api/routers/notification_preferences.py` | WARNING | fixed | No logging in any of the three new endpoints, unlike `notifications.py` and both event handlers. No audit trail for "I enabled it but got nothing" support/debugging. |
| R4-004 | resilience | `config.py`, `.env.example` | WARNING | fixed | No startup warning if the old `NOTIFICATION_MOVEMENT_THRESHOLD_METERS` env var is still set while the new `DEFAULT_...` var is absent — silent revert to the 50m default with no alerting signal. |
| R4-005 | resilience | `alembic/versions/r5s6t7u8v9w0_*.py` | SUGGESTION | info | Unbatched backfill INSERT — explicitly accepted in design.md given current project scale. No action needed. |
| R2-001 | readability | `notification_dispatch_handler.py`, `ser_ticket_trigger_handler.py` | WARNING | fixed | `_find_preference`/`_effective_threshold` duplicated byte-for-byte across both handlers — drift risk on future changes. |
| R2-002 | readability | `presentation/api/routers/notification_preferences.py` (`_find_type`) | SUGGESTION | fixed | Third instance of the same hand-rolled linear-search pattern as R2-001. |
| R2-003 | readability | `domain/exceptions.py` (`NotificationTypeNotFoundError`) | WARNING | fixed | Defined, never raised or caught anywhere; router uses a raw `HTTPException(404)` instead. Dead code, misleading to future readers. Duplicate of R3-005. |
| R2-004 | readability | `notification_preferences_repo.py` (`ensure_defaults`) vs `user_preferences_repository.py` (`ensure_default`) | SUGGESTION | open | Near-identical names (singular/plural) for semantically different operations, called back-to-back at the login call site — typo-prone. Intentional per design.md decision 7. |
| R2-005 | readability | `frontend/src/pages/PreferencesPage.tsx` (diff-based PUT) | WARNING | rejected | Flagged as unnecessary complexity vs. "PUT everything on save." Rejected: the diffing is load-bearing for correctness — it prevents silently converting every user's untouched fallback-default config into a pinned override on first save (design.md Decision 3). Simplifying as suggested would be a regression. |
| R2-006 | readability | `frontend/src/pages/PreferencesPage.tsx` (`as Record<string, number>` casts) | SUGGESTION | info | Unchecked narrowing casts; only matters once `config_schema` grows a non-numeric field. No action needed today. |
| R3-001 | reliability | `frontend/e2e/preferences.spec.ts` | BLOCKER | fixed | New endpoints (`/api/notifications/types`, `/api/notifications/preferences`) aren't mocked; they 401 against the real backend in CI, `Promise.all` rejects, and tests pass only because the error-state UI happens to coincide with expected defaults — not because load succeeded. |
| R3-002 | reliability | `frontend/src/pages/PreferencesPage.tsx` (Notifications section) | CRITICAL | fixed | Zero test coverage for the new section's own behavior. None of spec.md's three frontend scenarios (toggle off, edit threshold, hide config when disabled) are asserted anywhere. |
| R3-003 | reliability | `domain/value_objects/notification_config_schema.py` | WARNING | fixed | No test for the bool-vs-integer guard or the exact `value == min` boundary case. |
| R3-004 | reliability | `ser_ticket_trigger_handler.py` (`if zone is None: return`) | SUGGESTION | fixed | Defensive branch never exercised by any test. |
| R3-005 | reliability | `domain/exceptions.py` (`NotificationTypeNotFoundError`) | SUGGESTION | fixed | Duplicate of R2-003. |
| R3-006 | reliability | `tests/application/use_cases/test_authenticate_google_user.py` | SUGGESTION | info | New test doesn't assert `ensure_defaults` call ordering relative to `upsert`, unlike the file's existing pattern for `ensure_default`. Low behavioral risk. |
| R3-007 | reliability | `frontend/e2e/notification-channels.spec.ts` | BLOCKER | fixed | Discovered while fixing R3-001: this sibling spec has its own independent mock setup that also never mocked the two new endpoints, causing its "connect → preferred → disconnect" lifecycle test to time out (same root cause as R3-001, different file, since it also renders `PreferencesPage`). Fixed by adding matching GET-only mocks; full suite (14 tests across both files) now passes. |
| R3-008 | reliability | `alembic/versions/p3q4r5s6t7u8_create_notification_types.py` | BLOCKER | fixed | Discovered by the user manually testing the deployed app: `GET /notifications/types` returned 500 in the real docker-compose stack, so the Notifications section never rendered (`notificationTypes.length > 0` gate never passed) — the exact "no menu in the UI" report. Root cause: `_THRESHOLD_CONFIG_SCHEMA = json.dumps({...})` was a Python **string**, and the JSONB adapter serialized it *again* on insert, so the column held a JSON string value instead of a JSON object — round-tripped back as `str`, failing `NotificationTypeResponse` pydantic validation on every request. None of the automated tests caught this because `test_notification_preferences_repo_integration.py`'s fixture hand-rolls its own schema/seed data (with a correctly-cast literal) instead of exercising this migration file — so the actual buggy code path was never executed by any test. Fixed the migration to use a plain dict, corrected the already-corrupted data in the live docker environment via direct SQL, and added `tests/test_notification_types_migration_seed.py` — a DB-free unit test that imports the migration module directly and asserts the seed value is a dict, closing the specific gap that let this ship. |
