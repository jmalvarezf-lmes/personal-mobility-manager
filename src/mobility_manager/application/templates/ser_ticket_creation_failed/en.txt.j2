{% if possibly_created %}We may have already created your SER ticket for zone {{ zone_number }}, but couldn't confirm it — please check your provider account before creating another one manually.{% else %}We couldn't automatically create your SER ticket for zone {{ zone_number }} — please create it manually.{% endif %}